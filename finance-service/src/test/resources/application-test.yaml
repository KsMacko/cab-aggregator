spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: none
    show-sql: false
  liquibase:
    enabled: false
  messages:
    basename: messages
    encoding: UTF-8